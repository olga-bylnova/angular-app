import { StockLabelDirective } from './stock-label.directive';

describe('StockLabelDirective', () => {
  it('should create an instance', () => {
    const directive = new StockLabelDirective();
    expect(directive).toBeTruthy();
  });
});
